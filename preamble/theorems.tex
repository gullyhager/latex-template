% filepath: preamble/theorems.tex

% amsthm is already loaded by newpxmath — do not load again
\usepackage{mdframed}
\usepackage{relsize}
\usepackage{etoolbox}

% ── Custom Theorem Styles ─────────────────────────────────────

\newtheoremstyle{elegant-plain}
    {12pt}{8pt}
    {\normalfont}{}
    {\bfseries\scshape}{.}
    {0.5em}{}

\newtheoremstyle{elegant-definition}
    {12pt}{8pt}
    {\normalfont}{}
    {\bfseries\scshape}{.}
    {0.5em}{}

\newtheoremstyle{elegant-remark}
    {8pt}{8pt}
    {\normalfont}{}
    {\itshape}{.}
    {0.5em}{}

\newtheoremstyle{elegant-problem}
    {12pt}{8pt}
    {\normalfont}{}
    {\bfseries\scshape}{.}
    {0.5em}{}

\newtheoremstyle{elegant-solution}
    {8pt}{8pt}
    {\normalfont}{}
    {\itshape}{.}
    {0.5em}{}

% ── Assign Styles ─────────────────────────────────────────────
\theoremstyle{elegant-plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}

\theoremstyle{elegant-definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}
\newtheorem{nonexample}[theorem]{Non-Example}

\theoremstyle{elegant-remark}
\newtheorem{remark}[theorem]{Remark}
\newtheorem*{note}{Note}

\theoremstyle{elegant-problem}
\newtheorem{problem}{Problem}

\theoremstyle{elegant-solution}
\newtheorem*{solution}{Solution}

% ── Proof Styling ─────────────────────────────────────────────
\renewcommand{\proofname}{\normalfont\scshape Proof}

% ── mdframed Shared Style ─────────────────────────────────────
\mdfsetup{
    skipabove         = 10pt,
    skipbelow         = 8pt,
    innertopmargin    = 6pt,
    innerbottommargin = 8pt,
    innerleftmargin   = 10pt,
    innerrightmargin  = 6pt,
    linewidth         = 1.2pt,
    rightline         = false,
    topline           = false,
    bottomline        = false
}

% ── Separator after each problem ──────────────────────────────
\AtEndEnvironment{problem}{%
    \vspace{4pt}%
    \begin{center}\rule{0.5\textwidth}{0.4pt}\end{center}%
}